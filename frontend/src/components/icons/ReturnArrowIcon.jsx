import React from "react";
import { BsArrowReturnLeft } from "react-icons/bs";

const ReturnArrowIcon = ({ customStyle }) => {
  return <BsArrowReturnLeft className={customStyle} />;
};

export default ReturnArrowIcon;
